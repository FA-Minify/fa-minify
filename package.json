{
  "name": "fa-minify",
  "description": "Core functions of the FA-Minify Library",
  "authors": "NetWin",
  "license": "MIT",
  "version": "5.8.3",
  "main": "dist/index.js",
  "keywords": [
    "FontAwesome",
    "Minify"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/FA-Minify/fa-minify"
  },
  "scripts": {
    "clean": "rimraf dist",
    "pretest": "rimraf ./src/**/*.js ./tests/**/*.js && tsc -p ./tests",
    "test": "jasmine ./tests/**/free/*.spec.js",
    "prepublish": "npm run clean && tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jasmine": "^3.10.3",
    "@types/node": "^16.11.24",
    "jasmine": "^4.0.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
